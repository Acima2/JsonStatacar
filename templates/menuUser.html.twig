<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('user') }}">Accueil</a>
            </li>
            {% if not app.user.hasActiveDeplacement %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('new-deplacement') }}">Départ</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('back-deplacement') }}">Retour</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('plein-carburant') }}">Plein carburant</a>
            </li>
            {% endif %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="{{ path('accueil') }}" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    User
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="#">Votre profil</a>
                    <a class="dropdown-item" href="#">Déconnexion</a>
                </div>
            </li>
        </ul>
    </div>
</nav>