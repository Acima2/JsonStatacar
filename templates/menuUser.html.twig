<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('user_accueil') }}">Accueil</a>
            </li>
            {% if not app.user.hasActiveDeplacement %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('new-deplacement') }}">Départ</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('back-deplacement') }}">Retour</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('plein-carburant') }}">Plein carburant</a>
                </li>
            {% endif %}
            <li class="nav-item">
                    <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
            </li>
        </ul>
        {% if is_granted('ROLE_ADMIN') %}
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>